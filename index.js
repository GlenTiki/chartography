module.exports = function Chartography (input) {
  // maybe store some old values in a local db, and dump them into the html
  // so we can render multiple values on the same graphs?
  // create a graph
}
